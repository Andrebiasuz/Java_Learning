public interface iWanabaSutobo
{
    // we dont public because all interfaces are public anyway.
    // we only use methods here, because interfaces are windows to methods in the classes. .

    void professionPO();
}
